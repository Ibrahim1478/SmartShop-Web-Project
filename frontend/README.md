# SmartShop Frontend (React + Material UI)

## Run
```bash
npm install
npm start
```

Backend should run at `http://localhost:8080`.
If your backend uses a different URL, set:
```bash
REACT_APP_API_BASE_URL=http://localhost:8080/api
```
